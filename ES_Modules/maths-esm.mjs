//ES Module (EcmaScript)

// const add = (a, b) => {
//     return a + b;
// };

// export default add;

export const add = (a, b) => {
    return a + b;
}

export const subtract = (a, b) => {
    return a - b;
}
