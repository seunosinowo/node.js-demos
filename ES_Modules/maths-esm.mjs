//ES Module (EcmaScript)

// const add = (a, b) => {
//     return a + b;
// };

// export default add;

const add = (a, b) => {
    return a + b;
}

const subtract = (a, b) => {
    return a - b;
}

export default{
    add,
    subtract
}